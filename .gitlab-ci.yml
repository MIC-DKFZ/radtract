stages:
  - test

test:pytest:
  image: python:3.10
  stage: test
  before_script:
#    - python -m venv testenv
#    - source testenv/bin/activate
    - pip install --upgrade pip
    - pip install numpy
    - pip install pytest
  script:
    - pip install .
    - pytest tests/

